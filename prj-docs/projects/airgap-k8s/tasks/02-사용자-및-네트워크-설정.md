# 02. 사용자 및 네트워크 설정

## Status
- Chapter Status: TODO

## Scope
- `devops` 사용자 생성
- sudo 권한 부여
- `k8s-master`, `k8s-worker1` hostname 설정
- `/etc/hosts` 등록

## Confirmed TODO
- VM 대상 노드 기준 확정
- 실제 노드 IP 확정
- 사용자/hostname/hosts 설정 스크립트 또는 명령 정리
- 검증 결과 기록
- `ops/02-user-network/scripts/configure-node-user-network.sh`, `ops/02-user-network/scripts/verify-node-user-network.sh` 기준으로 실행 절차 정리
- 제출 매뉴얼은 `02-01.1`~`02-01.7` 번호 기준 유지

## Sidecar Draft
- 실제 노드 접속 전까지 아래 내용은 제출 매뉴얼에 넣지 않는다.
- 실행 완료 후 명령과 결과를 `manual/02-사용자-및-네트워크-설정/README.md`에 반영한다.
- 이 장부터는 VM 노드를 기준으로 진행한다.

```bash
sudo useradd -m -s /bin/bash devops
sudo passwd devops
sudo usermod -aG sudo devops
id devops
sudo -l -U devops
```

```bash
sudo hostnamectl set-hostname k8s-master
sudo hostnamectl set-hostname k8s-worker1
sudo tee -a /etc/hosts >/dev/null <<'EOF'
<MASTER_PRIVATE_IP>  k8s-master
<WORKER1_PRIVATE_IP> k8s-worker1
EOF
getent hosts k8s-master
getent hosts k8s-worker1
```
